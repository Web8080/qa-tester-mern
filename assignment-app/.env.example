# MongoDB (use either MONGODB_URI or DATABASE_URL; seed uses MONGODB_URI first)
MONGODB_URI=mongodb://localhost:27017/buggy-mern-app
DATABASE_URL=mongodb://localhost:27017/buggy-mern-app

# JWT (use strong secrets in production)
JWT_SECRET=weak-secret-key-123
REFRESH_TOKEN_SECRET=weak-refresh-secret-456

NODE_ENV=development
PORT=3001

# Frontend API base URL (for Vite dev server)
VITE_API_BASE=http://localhost:3001
